export class ATDocument {
    title: string
    type: string
    description
    contents: string

    constructor(title: string, type: string, description: string, contents: string) {
        this.title = title, 
        this.type = type
        this.description = description, 
        this.contents = contents
    }
}